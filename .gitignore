# Local MCP config carries credentials — never commit it. Commit the template.
.mcp.json

# Vendored upstreams cloned by install.sh — generated, never source. Kept out
# of git here and in any submodule checkout (install.sh also teaches the host
# project's code index to skip them; see ensure_host_ignores in install.sh).
vendor/

.DS_Store
